# direct dotnet sonar scanner

Docker image with java used especially with sonarqube scanner.

```bash

docker build . -t valda/dotnet-sonarscanner:1
docker push valda/dotnet-sonarscanner:1

```